# Next Tasks

## Priority 1
- Implement auto-match for same-name `.pdf` when `.typ` is selected from a document tree.
- Add explicit status messages for match success/failure and fallback actions.

## Priority 2
- Replace compile placeholder button with a compile pipeline interface.
- Start with a pluggable service contract (local/remote), then wire first implementation.

## Priority 3
- Improve preview UX:
  - Persist last opened typ/pdf URIs
  - Remember last PDF page index per file
  - Add basic loading/error state for PDF rendering
